services:
  postgres:
    image: postgres:16-alpine
    container_name: obra-expenses-postgres
    environment:
      POSTGRES_DB: obra_expenses
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d obra_expenses"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:

